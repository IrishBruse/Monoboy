<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Library so self-contained apps (e.g. Monoboy.Desktop) can reference this assembly. -->
    <OutputType>Library</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
